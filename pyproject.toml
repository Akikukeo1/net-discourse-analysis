[project]
name = "net-discourse-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "logging>=0.4.9.6",
    "psutil>=7.2.2",
    "ruff>=0.15.13",
]

[dependency-groups]
dev = [
    "ty>=0.0.37",
]
